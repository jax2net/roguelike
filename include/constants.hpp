#ifndef CONSTANTS_H
#define CONSTANTS_H

#include <string>

const size_t WIN_WIDTH = 800;
const size_t WIN_HEIGHT = 800;

// Map dimentions
const size_t map_w = 16;
const size_t map_h = 16;
const size_t block_w = 50;

#endif
